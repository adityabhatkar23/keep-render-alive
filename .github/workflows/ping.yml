name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:       


jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping your Render backend
        run: |
          curl -s https://sync-watch-jo2i.onrender.com/health || echo "Ping failed"
